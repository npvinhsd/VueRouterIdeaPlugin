import com.intellij.openapi.diagnostic.Logger;
import org.jetbrains.annotations.NotNull;

public class RouteLogger {
    public final static @NotNull Logger LOG = Logger.getInstance(RouteLogger.class);
}
